import { Button } from "../Button";
import IconLanguages from '../../assets/languages.svg?react';

export const LanguagesButton = () => {
  const onClickHandler = () => {

  }

  return <Button SvgIco={IconLanguages} onClick={onClickHandler}/>;
}
